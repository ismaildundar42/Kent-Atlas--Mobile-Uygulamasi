<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TurkiyeKentlerininSimgeleri"
        tools:targetApi="31">

        <!-- Bu meta-data öğesinin çıkarılması gerekebilir veya doğru bir şekilde tanımlanmalıdır -->
        <!--
 <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/button_background" />
        -->
        <activity
            android:name=".MainSehirDetayBilgi"
            android:exported="false" />
        <activity
            android:name=".MainSehirBilgi"
            android:exported="false" />
        <activity
            android:name=".MainListe"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>